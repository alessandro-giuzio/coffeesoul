---
export interface Props {
  title?: string;
  text?: string;
  img?: string;
  imageAlt?: string;
  pubDate?: string;
}

const { title, text, img, imageAlt, pubDate } = Astro.props as Props;
---

<article>
  <h1>{title}</h1>
  <p>{text}</p>
  <img src={img} alt={imageAlt} />
</article>
